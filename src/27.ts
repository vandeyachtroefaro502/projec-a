// This is an example of using Git and GitHub for version control.
// Make sure to set your local environment variables first before running the script.

import { cloneDeep } from 'lodash';

const main = () => {
  console.log('Running main function');
};

main();
